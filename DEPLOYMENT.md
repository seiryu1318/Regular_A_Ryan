# 배포 절차

현재 화면 버전은 `app/release.ts`에서 관리한다.

1. 요청 사항을 반영한다.
2. 다음 버전을 준비한다. 최초 공개는 `ver.00`이며 이후 성공한 공개마다 1씩 올린다.
3. `pnpm run build:html`을 실행한다. 이 과정에서 정시 변화, 반영방법, 3개년 입시결과, 일정 전체와 대학어디가, 대학 공식 입학처, 시도교육청 공개자료 검사가 모두 통과해야 한다.
4. 생성된 `index.html`과 `2027학년도_정시모집_전형변화와_입시결과.html`을 확인한다.
5. 검증된 변경만 `https://github.com/seiryu1318/Regular_A_Ryan.git`의 `main` 브랜치에 푸시한다.
6. GitHub Pages 배포가 완료된 뒤 공개 주소에서 제목, 버전, 로고, 네 탭과 주요 상호작용을 확인한다.
7. 검사 또는 공개 확인이 실패하면 제안한 버전을 확정하지 않는다.

시스템 종료 명령은 실행하지 않는다.
